apiVersion: v1
kind: Service
metadata:
  name: {{ include "yarpgateway.fullname" . }}
  labels:
    {{- include "yarpgateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yarpgateway.selectorLabels" . | nindent 4 }}
